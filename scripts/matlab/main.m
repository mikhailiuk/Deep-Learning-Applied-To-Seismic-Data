    %clear all the variables before executing the code
    clear

    addpath('./additional/');
    addpath('./correlation/');
    addpath('./dataProcessing/');
    addpath('./dataProcessing/whitening/');
    addpath('./featureProcessing/');        
    addpath('./plotFunctions/');
    addpath('./sequentialInfoResults/');

    %name the directory to clean
    nameDir = '../../output/';
    
    
    plotNoSubDir(nameDir);
%    plotOneSubDir(nameDir);
%    plotThreeSubDir(nameDir);
  
    
    
        




